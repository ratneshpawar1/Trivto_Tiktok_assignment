export default function Home() {
  return (
    <main
      style={{
        height: "var(--slide-height)",
        display: "grid",
        placeItems: "center",
        color: "var(--muted)",
      }}
    >
      Photo feed — coming soon.
    </main>
  );
}
